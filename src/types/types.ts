/**
 * @type rpm = revolutions per minute, in number form
 */
type rpm = number;
/**
 * @type speedMS = speed in meter per second
 */
type speedMPS = number;
/**
 * @type speedKMH = speed in kilometer per hour
 */
type speedKMPH = number;

type rad = number;
type newton = number;
type mps = number;

type force = number;
